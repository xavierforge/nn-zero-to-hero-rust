[package]
name = "gpt"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "~0.19", features = [
    "std",
    "tui",
    "train",
    "wgpu",
    "fusion",
], default-features = false }
thiserror = { workspace = true }
